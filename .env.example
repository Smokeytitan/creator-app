# Environment Variables Example - Isolated Version
# This version uses localStorage only - no external services required

# The isolated version does not require any API keys or tokens
# All data is stored in browser localStorage
